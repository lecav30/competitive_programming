#include <bits/stdc++.h>
using namespace std;

void solve() {
    int k, n;
    cin >> k >> n;
    cout << (k / n) * (k / n) << "\n";
}

int main() {
    int tc;
    cin >> tc;
    while (tc--) solve();
    return 0;
}
