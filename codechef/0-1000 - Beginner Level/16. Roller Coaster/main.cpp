#include <bits/stdc++.h>
using namespace std;

#define endl "\n"

void solve() {
    int x, h;
    cin >> x >> h;
    cout << (x >= h ? "YES" : "NO") << endl;
}

int main() {
    int tc;
    cin >> tc;
    while (tc--) {
        solve();
    }
    return 0;
}
